name: Snyk Security Scan

# Trigger only on default branch
on:
  push:
    branches:
      - main        # Replace with your default branch name if different
  pull_request:
    branches:
      - main        # Runs on PRs targeting main
  workflow_dispatch: # Optional: manual trigger from Actions tab

jobs:
  snyk-sca:
    runs-on: ubuntu-latest

    steps:
      # Checkout repository
      - name: Checkout code
        uses: actions/checkout@v3

      # Set up JDK 17 with Maven cache
      - name: Set up JDK 17 with Maven
        uses: actions/setup-java@v3
        with:
          distribution: temurin
          java-version: 17
          cache: maven

      # Verify Maven installation
      - name: Verify Maven
        run: mvn -version

      # Install Snyk CLI
      - name: Install Snyk
        run: npm install -g snyk

      # Authenticate Snyk
      - name: Authenticate Snyk
        run: snyk auth ${{ secrets.SNYK_TOKEN }}

      # Run Snyk test using system Maven
      - name: Snyk Test for Maven
        working-directory: JtProject
        run: snyk test --maven-executable=mvn --severity-threshold=medium || echo "Vulnerabilities found, continuing workflow."

      # Send results to Snyk dashboard
      - name: Snyk Monitor
        working-directory: JtProject
        run: snyk monitor --maven-executable=mvn
